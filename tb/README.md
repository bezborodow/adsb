# Behavioural Testbenches

These are behavioural testbenches that require Vunit and GHDL.

Run them from the project root directory:

```
./generate_vunit_data
./vunit
```

For help:

```
./vunit --help
```
